---
const { class: className = "", ...props } = Astro.props;
---

<article
  class:list={[
    "overflow-hidden transition duration-300 hover:-translate-y-0.5 hover:border-black/70 hover:saturate-105",
    className,
  ]}
  {...props}
>
  <slot />
</article>
