---
import Button from "../Button.astro";
import type { Props as ButtonProps } from "../Button.astro";

interface Props extends ButtonProps {};

const { text, link, id} = Astro.props;
---

<Button id={id} text={text} link={link} class="m-2" />
